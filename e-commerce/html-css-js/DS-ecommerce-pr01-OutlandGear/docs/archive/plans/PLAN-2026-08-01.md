# OutlandGear — Development Plan

**Last reviewed:** 2026-08-01
**Project type:** Static multi-page e-commerce demo frontend (semantic HTML, layered CSS, Vanilla JavaScript ES modules, local JSON data, Node.js build pipeline, Netlify deployment)
**Plan status:** Completed

## Planning principles

- The plan reflects the current verified project state.
- Main items are checked only when all required subtasks are complete.
- Generated output (`dist/`) is not treated as canonical source.
- Completed significant changes are recorded separately in `CHANGELOG.md`.
- Findings originate from `docs/archive/audits/daily-AUDIT-2026-08-01.md`; only currently open findings appear as active work.

## Current priorities

No active implementation work. All audit findings tracked in this plan are closed.

1. `PH2-06` — Awaiting a project-owner decision on the `aria-disabled` / `pointer-events` rule; no implementation is scheduled until that decision is made.

## Phase 1 — Verified foundation (completed)

**Goal:** Establish canonical product-data loading, resolve known dark-theme contrast defects, and add baseline repository tooling.

- [x] **PH1-01 — Consolidate product-data loading through one validated loader**
  - `cart.js`, `catalog.js`, `product.js`, and `travel-kits.js` now share `loadNormalizedProducts` from `product-data.js` instead of four duplicated fetch paths.

- [x] **PH1-02 — Enforce the product-page quantity limit before adding to cart**
  - The add-to-cart handler clamps the requested quantity to the field's own `max` attribute and surfaces the clamp via a toast.

- [x] **PH1-03 — Fix dark-theme toast contrast and cart-save failure severity**
  - Added dedicated `--color-toast-bg` and `--color-toast-info-border` tokens; the cart-save failure now renders as an error toast instead of the implicit info fallback.

- [x] **PH1-04 — Add `.gitattributes` line-ending enforcement**
  - Normalizes line endings to LF at the Git level (`* text=auto eol=lf`).

- [x] **PH1-05 — Declare supported Node.js versions**
  - Added an `engines.node` range (`>=20 <23`) in `package.json`, covering the CI-pinned version and the local development version.

## Phase 2 — Accessibility and maintenance work (completed)

**Goal:** Close the open audit findings from the P2 series. All implementation items are complete; one finding remains open as a project-owner decision.

- [x] **PH2-01 — Extend accessibility test coverage to interaction-only component states**
  - [x] add at least one interaction-triggered scan for the toast component, the variant already found to fail
  - [x] evaluate whether the same coverage is warranted for the nav drawer, search panel, legal modal, and state banners
  - [x] keep the existing resting-state route scans unchanged
  - **Priority:** Medium — this gap already let two contrast defects reach production undetected
  - **Completion condition:** at least the toast component's accessible states are exercised and scanned by the automated suite, not only its resting DOM state
  - **Source:** `docs/archive/audits/daily-AUDIT-2026-08-01.md` — P2-05

- [x] **PH2-02 — Collapse the duplicate `data.js` module instance**
  - [x] align `js/modules/travel-kits.js`'s import specifier for `data.js` with `js/modules/product-data.js`'s
  - [x] verify both modules resolve to the same module instance after the change
  - **Priority:** Low — explicitly not a defect; `product-data`'s caching is unaffected
  - **Completion condition:** `data.js` is imported through one identical specifier across the project
  - **Source:** `docs/archive/audits/daily-AUDIT-2026-08-01.md` — P2-04

- [x] **PH2-03 — Fix disabled-button text contrast in the light theme**
  - Added the `--color-btn-disabled-text` token in both theme blocks and raised the disabled-state opacity, bringing the contact and newsletter submit buttons above the 4.5:1 threshold.
  - **Source:** `docs/archive/audits/daily-AUDIT-2026-08-01.md` — P2-06

- [x] **PH2-04 — Make the legal modal's state deterministic during resting-state scans**
  - Seeded the acceptance flag in `tests/a11y/a11y.spec.js` and added a per-scan assertion that the modal is closed, removing a race against its 700 ms auto-open timer.
  - **Source:** `docs/archive/audits/daily-AUDIT-2026-08-01.md` — P2-07

- [x] **PH2-05 — Add a busy state to the checkout submit button**
  - Routed checkout submission through the existing `setSubmitState` helper, and added a `pageshow` guard in all three form modules so the busy state clears on bfcache restore.
  - **Source:** `docs/archive/audits/daily-AUDIT-2026-08-01.md` — P2-09

- [-] **PH2-06 — Decide whether `pointer-events: none` should apply to the `aria-disabled` branch**
  - Deferred: no runtime effect today since `aria-disabled` is unused. Revisit when `aria-disabled` is first introduced anywhere in the project.
  - **Source:** `docs/archive/audits/daily-AUDIT-2026-08-01.md` — P2-10
